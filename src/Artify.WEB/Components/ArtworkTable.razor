@if (Artworks.Count() > 0){
    <div class="row mt-3">
        @foreach (var artwork in Artworks)
        {
            <div class="col-md-3 mb-2" style="padding: 2px;">
                <a href="#">
                    <img class="img-thumbnail" src="@artwork.ImageUrl" >
                </a>
            </div>
        }
    </div>
}
else
{
    <span>
        Loading artworks...
    </span>
}
